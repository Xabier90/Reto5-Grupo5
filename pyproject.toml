[tool.pytest.ini_options]
testpaths = ["proyecto/tests"]

name = "reto5"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "mysql-connector-python>=8.0",
    "werkzeug>=3.0.0"
]